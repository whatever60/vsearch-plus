# Data Manifests

Keep small tracked notes here about external datasets, versions, URLs, and checksums.

Large downloaded archives and extracted assets belong elsewhere under `data/` and stay out of Git.
