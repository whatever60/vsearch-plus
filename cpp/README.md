# C++ Component

This directory contains the paired-end extended VSEARCH source tree and its autotools metadata.

Use `../scripts/build_cpp.sh` from the repository root to build out of tree into `build/cpp/`.
