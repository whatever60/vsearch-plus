# Tests

This directory is reserved for repository-level smoke, unit, and integration tests.

The existing parity and behavior checks are currently documented under `docs/` and exercised through local command runs.
Test files can be moved here as the conda and CI layout solidifies.
